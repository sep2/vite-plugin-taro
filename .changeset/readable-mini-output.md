---
'vite-plugin-taro': patch
---

调整小程序默认 JavaScript 输出布局：移除文件名中的内容哈希，App、Page 胶囊与原生入口同目录，共享代码输出到 `common/`。保留现有 React/Taro vendor 分组，应用代码与其他依赖仍由 Vite/Rolldown 自动分块。导入资源沿用 Vite 的资源处理规则。监听构建只在启动时清理文件并保留目录，后续构建直接覆盖产物，不再逐轮清空；过时文件保留到重启监听或执行干净构建。

### 升级说明

升级后重启开发或监听命令，发布前执行一次完整构建。如有脚本直接引用旧的 `assets/*-<hash>.js`，请按新目录结构调整。原生页面路由无需修改。
