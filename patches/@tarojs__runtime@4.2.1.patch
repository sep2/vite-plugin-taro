diff --git a/dist/bom/document.js b/dist/bom/document.js
index abb2d3e..423a5c6 100644
--- a/dist/bom/document.js
+++ b/dist/bom/document.js
@@ -1,5 +1,5 @@
 import { isEnableTTDom, DEFAULT_COMPONENTS, TT_SPECIFIC_COMPONENTS } from '@tarojs/shared';
-import { HTML, HEAD, BODY, APP, CONTAINER } from '../constants/index.js';
+import { HTML, HEAD, BODY, APP, CONTAINER, ROOT_STR } from '../constants/index.js';
 import { TaroDocument } from '../dom/document.js';
 import env from '../env.js';

@@ -10,7 +10,7 @@ function createDocument() {
        *     <head></head>
        *     <body>
        *       <container>
-       *         <app id="app" />
+       *         <root id="app" />
        *       </container>
        *     </body>
        *   </html>
@@ -21,7 +21,19 @@ function createDocument() {
     const html = documentCreateElement(HTML);
     const head = documentCreateElement(HEAD);
     const body = documentCreateElement(BODY);
-    const app = documentCreateElement(APP);
+    /*
+     * App hosts are above native Page roots, so an ordinary TaroElement would have no _root scheduler and its React
+     * mutations would remain memory-only. Construct the pre-existing #app host as a TaroRootElement instead. It remains the
+     * same React container and keeps the same id, but can now batch app.* updates through its own ctx just as each Page root
+     * batches page.* updates. Reusing the host itself removes the need for a detached scheduler node.
+     *
+     * Taro and the framework locate this container by id; the pinned 4.2.1 runtime has no behavior branching on an APP
+     * nodeName or APP tagName. App snapshots hydrate the container's children rather than the container record itself, so
+     * ROOT is never emitted as a native layout node. The observable change is limited to unsupported code directly
+     * inspecting #app.nodeName/tagName or using this support fork with upstream templates. H5 loads the untouched
+     * runtime.esm.js entry and therefore retains the upstream <app> element.
+     */
+    const app = documentCreateElement(ROOT_STR);
     app.id = APP;
     const container = documentCreateElement(CONTAINER); // 多包一层主要为了兼容 vue
     doc.appendChild(html);
diff --git a/dist/dom/node.js b/dist/dom/node.js
index 415aa2e..e75885f 100644
--- a/dist/dom/node.js
+++ b/dist/dom/node.js
@@ -277,6 +277,15 @@ class TaroNode extends TaroEventTarget {
         return this.childNodes.length > 0;
     }
     enqueueUpdate(payload) {
+        /*
+         * The outlet is the structural boundary between singleton App hosts and Page roots in Taro's in-memory tree. Its
+         * children must remain attached there for React ancestry, event ownership, unmounting, and Taro parent links, while
+         * each Page root independently schedules its own page.* native updates. Allowing outlet child mutations to bubble
+         * into the App scheduler would fan Page topology out as app.* data and duplicate work across every mounted Page.
+         * Suppress only this native update propagation; do not change the in-memory childNodes relationship.
+         */
+        if (this.nodeName === 'vpt_page_outlet')
+            return;
         var _a;
         (_a = this._root) === null || _a === void 0 ? void 0 : _a.enqueueUpdate(payload);
     }
diff --git a/dist/dom/root.js b/dist/dom/root.js
index 727cf71..4e5bb3f 100644
--- a/dist/dom/root.js
+++ b/dist/dom/root.js
@@ -1,5 +1,5 @@
 import { hooks, isFunction, isArray, isUndefined } from '@tarojs/shared';
-import { ROOT_STR, SET_DATA, PAGE_INIT, CUSTOM_WRAPPER } from '../constants/index.js';
+import { ROOT_STR, SET_DATA, PAGE_INIT, CUSTOM_WRAPPER, CONTAINER } from '../constants/index.js';
 import { options } from '../options.js';
 import { perf } from '../perf.js';
 import { isComment, customWrapperCache } from '../utils/index.js';
@@ -49,7 +49,14 @@ class TaroRootElement extends TaroElement {
         this.tagName = ROOT_STR.toUpperCase();
     }
     get _path() {
-        return ROOT_STR;
+        /*
+         * Both the singleton App host and every Page host are now real TaroRootElement schedulers. Their structural parent
+         * distinguishes ownership without a mode flag: the App root is the sole root directly below the document's
+         * <container>, while Page roots live below vpt_page_outlet. Every descendant derives its setData path from this one
+         * prefix, yielding direct app.* and page.* payloads without host overrides or per-update path translation. nodeName
+         * remains root in both cases, preserving Taro's host, event, and scheduler semantics.
+         */
+        return this.parentNode && this.parentNode.nodeName === CONTAINER ? 'app' : 'page';
     }
     get _root() {
         return this;
@@ -71,8 +78,14 @@ class TaroRootElement extends TaroElement {
             const setDataMark = `${SET_DATA} 开始时间戳 ${Date.now()}`;
             perf.start(setDataMark);
             const data = Object.create(null);
+            /*
+             * During the first render Taro replaces the leading child array and removes redundant descendant writes from
+             * that same batch. For Page roots, _path emits page.*, so upstream's literal root.cn reset paths no longer match those queued
+             * payloads; the optimization would miss them and could also target a native key that does not exist. Align the
+             * two initial literals with page.cn so initial and steady-state scheduling obey one namespace.
+             */
             const resetPaths = new Set(initRender
-                ? ['root.cn.[0]', 'root.cn[0]']
+                ? ['page.cn.[0]', 'page.cn[0]']
                 : []);
             while (this.updatePayloads.length > 0) {
                 const { path, value } = this.updatePayloads.shift();
diff --git a/dist/hydrate.js b/dist/hydrate.js
index e3bd1f1..84b92fd 100644
--- a/dist/hydrate.js
+++ b/dist/hydrate.js
@@ -64,8 +64,16 @@ function hydrate(node) {
             compileModeName = props[prop];
         }
     }
-    // Children
-    data["cn" /* Shortcuts.Childnodes */] = node.childNodes.filter(node => !isComment(node)).map(hydrate);
+    /*
+     * The App scheduler hydrates this host tree into Page.data.app, but outlet.childNodes are the live Taro Page roots kept
+     * there to preserve one React/App ancestry. Traversing them would copy every mounted Page into every native Page's App
+     * mirror, cross the app/page scheduler boundary, and make Page changes invalidate App data. Serialize the outlet as an
+     * empty marker instead. Generated Page WXML fills that exact position from Page.data.page through its slot, while the
+     * in-memory Page roots remain untouched for lifecycle, event, and removal ownership.
+     */
+    data["cn" /* Shortcuts.Childnodes */] = nodeName === 'vpt_page_outlet'
+        ? []
+        : node.childNodes.filter(node => !isComment(node)).map(hydrate);
     if (node.className !== '') {
         data["cl" /* Shortcuts.Class */] = node.className;
     }
